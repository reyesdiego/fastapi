[Unit]
Description=Watchtower App
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/watchtower
ExecStart=/opt/watchtower/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target